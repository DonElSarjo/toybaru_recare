services:
  toybaru:
    build: .
    ports:
      - "8099:8099"
    volumes:
      - ${TOYBARU_HOST_DATA_DIR:-${HOME}/.config/toybaru}:/data
    environment:
      - TOYBARU_DATA_DIR=/data
    restart: unless-stopped
    dns:
      - 8.8.8.8
      - 8.8.4.4
